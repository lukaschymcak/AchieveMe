name = "achieveme-cloud-saves"
main = "src/index.ts"
compatibility_date = "2025-03-01"

# Location hints (eeur/weur/…) are NOT jurisdictions. Only eu|fedramp need a
# suffix endpoint. Default location-hint buckets use {account}.r2.cloudflarestorage.com.

# Secrets (wrangler secret put):
#   API_TOKEN
#   R2_ACCOUNT_ID
#   R2_ACCESS_KEY_ID
#   R2_SECRET_ACCESS_KEY
#   R2_BUCKET
